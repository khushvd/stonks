# Session Handoff — 2026-06-05

> **Purpose:** Bring the next session up to speed without re-reading everything. Read this before any other status file.
> **Generated by:** /handoff skill. **Audience:** the agent building B-full (multi-turn chat + cross-document synthesis) on top of the now-shipped B1 app.

## TL;DR — 5 things the next session must know

1. **B1 is DONE, reviewed (verdict: SHIP), and merged to `main`.** The local no-terminal web app works end-to-end: double-click launcher → `{company, ask}` → Run → live SSE coordinator → trust-aware dashboard. Manual golden-path E2E passed on Asian Paints. **103 tests green, tsc clean, `pnpm build` clean.**
2. **Everything is on `main` now.** The `phase2a-notebooklm-extractor` branch was merged (merge commit `4ce1bb4`) and **deleted**. There is **no git remote** configured — all work is local-only, nothing is pushed.
3. **The next phase is B-full** (out of scope for B1, by design): multi-turn conversational chat that refines/re-asks/adds companies mid-conversation, and surfaces NotebookLM **cross-document narrative synthesis** ("what did management say about pricing across the last 4 concalls?"). B1 built the rails — the `ask` field, the coordinator, the SSE stream. See design spec "Future scope".
4. **The app is READ-ONLY; only the coordinator mutates data.** The Next.js app never writes to `metrics`. The `/api/run` route spawns `claude -p` which runs the fixed pnpm chain (scrape→ingest→extract→verify→db summary) and that promotes metrics through the integrity gate. **Hazard:** any `/api/run` hit triggers a real, billable `claude -p` run that mutates `data/stonks.db`. When developing, point it at the fake: `CLAUDE_BIN=/tmp/fakebin/claude`.
5. **There is known, low-priority tech debt** (see "New work identified") — none of it blocks B-full, but the `listMetrics` full-scan and a couple of latent label/path mismatches are worth knowing before you extend the dashboard or coordinator.

## What was built this session

The entire B1 app (20 commits, `dfea35e`→`cf3ef0a`, plus merge `4ce1bb4`). Executed via subagent-driven-development (Sonnet implementers + reviewers, per-unit two-stage review + a final whole-implementation review).

| Area | Files | What |
|---|---|---|
| Coordinator | `src/coordinator/{types,stream,prompt,run}.ts` | stream-json→`AgentEvent` parser; injection-guarded prompt builder; injectable spawn wrapper with **AbortSignal→kill** (no orphaned billable runs on client disconnect) |
| Dashboard core | `src/dashboard/{trust,citation,data}.ts` | trust badge/integrity-chip presentation; traversal-hardened PDF path resolver + citation href; `getDashboard` shaping over `src/db` (no raw SQL) |
| API routes | `app/api/{dashboard,pdf,run}/route.ts` | read-only dashboard JSON; source-PDF stream w/ traversal guard; SSE coordinator stream. **All `runtime = "nodejs"`** |
| UI | `app/components/*.tsx` (10), `app/page.tsx` | Layout-A two-pane: control rail + live progress feed + trust dashboard (TrustBadge, IntegrityTile, MetricsTable, RejectsPanel, MarginChart) |
| Launcher | `scripts/stonks.command` | macOS double-clickable; plain-English preflight (claude on PATH? notebooklm auth? deps? db?) → build → start → open browser on port 4317 |
| Config | `next.config.mjs`, `tsconfig.json`, `app/globals.css` | `serverExternalPackages:["better-sqlite3"]`; webpack `extensionAlias` so `.js` specifiers resolve to `.ts`; dark trust-color CSS vars |
| Docs | `docs/superpowers/specs/2026-06-04-stonks-b1-app-design.md`, `docs/superpowers/plans/2026-06-04-stonks-b1-app.md` | the B1 design spec + 17-task TDD plan (both fully executed) |

## Actual build status

| Phase | Plan says | Reality | Action needed |
|---|---|---|---|
| 1 — pipeline | DONE | DONE | none |
| 2 — extraction + verifier (NotebookLM CLI) | DONE | DONE, E2E-verified | none |
| 2 — Next.js app + dashboards (**B1**) | was "remaining" | **DONE, merged to main, E2E-verified** | none — B1 complete |
| **B-full** — multi-turn chat + cross-doc synthesis | future | **not started** | **this is the next work** — needs its own brainstorm→spec→plan |
| 3 — Teacher agent + polish | future | not started | later |

No discrepancies between tracking files and code. `CLAUDE.md` still describes Phase 2's "Remaining" as "Next.js app + coordinator + dashboards" — that line is now stale (those are DONE); `/init` this session refreshes it.

`data/stonks.db` currently holds **2 companies / 8 filings / 3 verified metrics / 0 staging** (the manual E2E run re-scraped and added a second company's filings; the 3 verified Asian Paints metrics — revenue, pat, ebitda_margin, all p28 — are intact). `data/` is gitignored.

## Key findings

- **North star is genuinely delivered, not just present.** Final review traced the trust path end-to-end: `verified`→green `#00cc33` "VERIFIED", `notebooklm-only`→amber `#ffbb33` "NLM-ONLY", rejected→red-bordered Quarantine panel with reason+excerpt. The amber path is exercised by a real test (not dead code). A viewer can tell at a glance which numbers to trust.
- **Two reviewer findings were correctly DECLINED** (documented technical reasoning, not laziness): (a) "Dashboard needs `use client`" — backwards; in App Router a Server Component *can* import a Client Component, and `Dashboard` is already in the client subtree via `page.tsx`. (b) "Remove ProgressFeed's `done && !errored` clause" — that clause is load-bearing: the "Summarize" step emits no pnpm command, so without it that step would never tick green on a successful run.
- **A stray-server incident** (an earlier subagent crashed on an API ConnectionRefused and left a `pnpm start` bound to 4317 *without* `CLAUDE_BIN`) triggered one real billable coordinator run that polluted the DB with a duplicate `asianpaint` company. It was reverted to baseline with approval. Lesson encoded in TL;DR #4.

## New work identified (not yet in plan)

**B-full (the real next project)** — start with `/superpowers:brainstorming` off the existing design spec's "Future scope" section, then a fresh plan. Likely scope: multi-turn chat UI (the control rail already future-proofs Layout A for this), conversation state, NotebookLM cross-document narrative Q&A surfaced alongside the metrics table, ability to add companies mid-conversation.

**Tech debt (low priority, none blocks B-full):**
| # | Item | File | Note |
|---|---|---|---|
| 1 | `listMetrics(db)` fetches ALL metrics across all companies then filters in JS | `src/dashboard/data.ts` | table scan per dashboard load; fine at single-analyst scale, add `WHERE filing_id IN (...)` before multi-company B-full |
| 2 | `pnpm db *` step regex matches any db subcommand, not just `summary` | `src/coordinator/stream.ts` | latent mislabel if the coordinator ever calls `pnpm db stage/promote`; can't happen with today's fixed chain |
| 3 | `resolvePdfPath` returns the normalized (pre-realpath) path | `src/dashboard/citation.ts` | no security issue (realpath check guards escapes); read + check just operate on different path strings |
| 4 | `/api/pdf` buffers whole PDF into memory before responding | `app/api/pdf/route.ts` | fine for local PDFs; stream if files grow large |
| 5 | `.gitignore` may not cover `.DS_Store`, `.next/`, `next-env.d.ts` | repo root | verify/clean during `/init`; harmless but noisy |

## Before next session — action items for Khush

| Item | Exactly what to do | Urgency |
|---|---|---|
| Decide B-full scope | When ready, tell the next agent to brainstorm B-full (multi-turn chat + cross-doc synthesis). It needs your product direction on what the chat should do first. | Before B-full starts |
| Optional: set a git remote | If you ever want off-machine backup / PRs, create a GitHub remote and push `main`. Currently local-only. | Low |

## Open questions

| # | Question | Blocks | Status |
|---|---|---|---|
| 1 | What's the first B-full capability — multi-turn refine, or cross-document synthesis Q&A? | B-full plan scope | OPEN — Khush (product) |
| 2 | Keep the small background Terminal window, or build the menu-bar wrapper to hide it? | Launcher polish | OPEN (deferred to B-full/Phase-3 per spec) |
| 3 | Observable Plot confirmed for charts long-term? | Dashboard charts in B-full | OPEN (spec says Plot; only 1 chart in B1 so far) |

## Recommended first action next session

If building B-full: run `/superpowers:brainstorming` against `docs/superpowers/specs/2026-06-04-stonks-b1-app-design.md` ("Future scope" section) to scope the first B-full capability — **but clear Open-Q1 (which capability first) with Khush before writing the spec.** First verify the app still runs: `pnpm test` (expect 103 green), then `CLAUDE_BIN=/tmp/fakebin/claude pnpm dev` and open `localhost:4317`.

## File index — reading order

| Order | File | Why |
|---|---|---|
| 1 | `HANDOFF.md` (this) | Current state. |
| 2 | `CLAUDE.md` | Hard constraints (subscription-only, cheap models, integrity gate), Commands, Code map. |
| 3 | `docs/superpowers/specs/2026-06-04-stonks-b1-app-design.md` | B1 design + the trust north star + B-full future scope. |
| 4 | `docs/superpowers/plans/2026-06-04-stonks-b1-app.md` | The executed 17-task plan — exact shape of every B1 module. |
| 5 | `src/dashboard/data.ts` + `src/coordinator/run.ts` | The two seams B-full will extend (dashboard shaping, coordinator spawn/stream). |
| 6 | `app/page.tsx` + `app/components/ControlRail.tsx` | Layout A + SSE consumption — where multi-turn chat grafts on. |

## Reference plans

- `docs/superpowers/specs/2026-06-04-stonks-b1-app-design.md` — B1 design (UI-first, trust-layer north star, B-full future scope).
- `docs/superpowers/plans/2026-06-04-stonks-b1-app.md` — executed 17-task B1 plan.
- `docs/superpowers/specs/2026-06-04-notebooklm-cli-pivot-design.md` — the NotebookLM CLI pivot (background; done).
- `docs/superpowers/runs/2026-06-04-cli-pivot-asianpaint.md` — what the live extraction data looks like end-to-end.
